#include <stdio.h>
#include "main.h"

/**
 * main - main entry
 * @argc: input count
 * @argv: input array
 * Return: Always return 0 (success)
 */
int main(int argc, char *argv[])
{
	printf("%s/n", argv[0]);
	return (0);
}
