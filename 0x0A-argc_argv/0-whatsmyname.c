#include <stdio.h>
#include "main.h"

/**
 * main - main entry
 * Return: Always return 0 (success)
 */
int main (int argc, char *argv[])
{
	if (argc >= 1)
	{
		printf("%s/n", argv[0]);
	}
	return (0);
}
